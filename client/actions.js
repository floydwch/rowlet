import {createAction} from 'redux-actions'


export const receive_alarm = createAction('RECEIVE_ALARM')
export const read_alarm = createAction('READ_ALARM')
export const correct_prediction = createAction('CORRECT_PREDICTION')
export const expand_record = createAction('EXPAND_RECORD')
